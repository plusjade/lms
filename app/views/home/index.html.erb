<div id="NavTabs">
  <div class="primary-content">
    <div class="inner">
      <h3>Courses</h3>
      <ul>
      <% @courses.each do |course| %>
        <li><a href="<%= course_path(course) %>"><%= course.name %></a></li>
      <% end %>
      </ul>
    </div>
  </div>
</div>
